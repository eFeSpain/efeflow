node_modules/
dist/
dist-ssr/
*.local

src-tauri/target/
src-tauri/gen/

# Every icon here is derived from assets/app.png by `npm run icons`, which the
# app scripts run when the set is missing. Only the master is kept: the derived
# copies were drifting from what the generator produces, and since CI
# regenerates them before bundling, the ones committed were never the ones that
# shipped.
src-tauri/icons/

.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea/

# left behind only if the GIF recorder crashes mid-run
.gif-tmp/
